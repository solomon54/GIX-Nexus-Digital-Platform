# 04_PRODUCT

**Path:** `04_PRODUCT`

## Purpose
Working backlog / roadmap scratch space only. This folder deliberately overlaps in topic with spec chapters 01, 05, 06 — that's fine, because it is NEVER authoritative. It exists for day-to-day planning that hasn't gone through governance yet.

## Populate now?
Optional — only populate if useful for planning. Not required for the spec to be complete.

## Do NOT
Never cite this folder as a source of truth. Never let an agent treat content here as approved. The moment something here is decided, move it into 03_GOVERNANCE + the relevant spec chapter, then this copy becomes stale/removable.
